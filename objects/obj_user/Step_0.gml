if (position_meeting(mouse_x, mouse_y, id))
	msg = keyboard_string;
if (msg == "DEEZNUTS" && keyboard_check(vk_down))
	scr_sound(sound_youfuckindick);
if (position_meeting(mouse_x, mouse_y, id))
	selected = 1;
else
	selected = 0;
